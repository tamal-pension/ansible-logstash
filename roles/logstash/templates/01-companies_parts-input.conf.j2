input {
  jdbc {
    jdbc_driver_library => "{{ logstash_jdbc_driver_library }}"
    jdbc_driver_class => "{{ logstash_jdbc_driver_class }}"
    jdbc_connection_string => "{{ logstash_jdbc_connection_string }}"
    jdbc_user => "{{ logstash_jdbc_user }}"
    jdbc_password => "{{ logstash_jdbc_password }}"
    jdbc_paging_enabled => true
    jdbc_page_size => {{ logstash_jdbc_page_size }}
    schedule => "{{ logstash_schedule }}"
    last_run_metadata_path => "{{ logstash_last_run_metadata_basepath }}/.companies_parts_last_run"
    statement_filepath => "{{ logstash_last_run_statement_basepath }}/companies_parts.sql"
    jdbc_fetch_size => {{ logstash_jdbc_fetch_size }}
    tracking_column => "modify_date"
    tags => "cp_tag"
  }
}
